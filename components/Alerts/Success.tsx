import React from 'react';
import {MSG} from "../../shared";

const Success = ({ message }) => {
    return (
        <>
            <MSG>{message}</MSG>
        </>
    )
};

export default Success;
